import React from 'react'
import "./experience.css"
const Experience = ({scrollRef}) => {
  return (
    <div ref={scrollRef} className='experience-container' id="experience">Experience</div>
  )
}

export default Experience